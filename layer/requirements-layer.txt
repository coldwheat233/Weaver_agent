# FC 自定义层依赖 —— 重型库，构建一次，很少变
# 构建: pip install -r requirements-layer.txt -t python/lib/python3.11/site-packages/
# 打包: zip -r layer-weaver-deps.zip python/

# ASR
openai-whisper>=20231117

# 向量存储
chromadb>=0.5.0

# whisper 依赖
torch>=2.1.0
numpy>=1.26.0
pandas>=2.0.0

# 图分析
networkx>=3.3
